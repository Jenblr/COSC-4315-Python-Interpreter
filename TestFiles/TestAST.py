# Sample Python code
a = 5
b = 3
c = a + b * 2

# Tokenizing variables (a, b, c), integer literals (5, 3), and operators (=, +, *).
# Constructing assignment nodes for the assignment statements (a = 5, b = 3, c = a + b * 2).
# Constructing expression nodes for the arithmetic expression a + b * 2.